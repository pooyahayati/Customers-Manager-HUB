name: Channel Acceptance Temporary

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TEST_DATABASE_URL: postgresql+psycopg://cmh:change-me@127.0.0.1:5432/customers_manager_hub
      TEST_REDIS_URL: redis://127.0.0.1:6379/0

    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Install uv
        run: python -m pip install uv==0.12.10

      - name: Frozen dependency sync
        run: uv sync --frozen

      - name: Apply acceptance coverage
        run: |
          python .github/channel_acceptance_temp.py
          uv run ruff format apps/api/tests/test_telegram_channel_integration.py

      - name: Commit acceptance coverage
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/api/tests/test_telegram_channel_integration.py
          git commit -m "test: complete Telegram milestone acceptance coverage"
          git push origin "HEAD:${{ github.head_ref }}"

      - name: Ruff format check
        run: uv run ruff format --check apps/api/src apps/api/tests

      - name: Ruff lint
        run: uv run ruff check apps/api/src apps/api/tests

      - name: Pyright
        run: uv run pyright

      - name: Unit and connector tests
        run: uv run pytest

      - name: Start PostgreSQL and Redis
        run: docker compose -f compose.yaml up -d --build postgres redis

      - name: Wait for dependencies
        shell: bash
        run: |
          set -euo pipefail
          for _ in $(seq 1 60); do
            if docker compose -f compose.yaml exec -T postgres pg_isready -U cmh -d customers_manager_hub >/dev/null 2>&1 && docker compose -f compose.yaml exec -T redis redis-cli ping | grep -q PONG; then
              exit 0
            fi
            sleep 2
          done
          exit 1

      - name: Apply migrations
        run: DATABASE_URL="$TEST_DATABASE_URL" uv run alembic upgrade head

      - name: Verify migration metadata is current
        run: DATABASE_URL="$TEST_DATABASE_URL" uv run alembic check

      - name: Run PostgreSQL and Redis integration suite
        run: RUN_DB_INTEGRATION=1 DATABASE_URL="$TEST_DATABASE_URL" REDIS_URL="$TEST_REDIS_URL" uv run pytest

      - name: Cleanup
        if: always()
        run: docker compose -f compose.yaml down -v --remove-orphans
