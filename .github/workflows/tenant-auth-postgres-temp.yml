name: Tenant Auth PostgreSQL Temporary

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  tenant-auth-postgres:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      DATABASE_URL: postgresql+psycopg://cmh:change-me@127.0.0.1:5432/customers_manager_hub
      RUN_DB_INTEGRATION: "1"

    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Install uv
        run: python -m pip install uv==0.12.10

      - name: Frozen dependency sync
        run: uv sync --frozen

      - name: Start PostgreSQL
        run: docker compose -f compose.yaml up -d --build postgres

      - name: Wait for PostgreSQL
        shell: bash
        run: |
          set -euo pipefail
          for _ in $(seq 1 60); do
            if docker compose -f compose.yaml exec -T postgres pg_isready -U cmh -d customers_manager_hub >/dev/null 2>&1; then
              exit 0
            fi
            sleep 2
          done
          docker compose -f compose.yaml logs postgres
          exit 1

      - name: Apply migration from source environment
        run: uv run alembic upgrade head

      - name: Verify migration metadata is current
        run: uv run alembic check

      - name: Run PostgreSQL integration suite
        run: uv run pytest

      - name: Build backend image
        run: docker compose -f compose.yaml build api

      - name: Verify migration command inside backend image
        run: docker compose -f compose.yaml run --rm api alembic upgrade head

      - name: Start full application stack
        run: docker compose -f compose.yaml up -d --build

      - name: Verify full runtime
        shell: bash
        run: |
          set -euo pipefail
          for _ in $(seq 1 60); do
            if curl --fail --silent http://127.0.0.1:8000/ready >/dev/null; then
              break
            fi
            sleep 2
          done
          curl --fail --silent http://127.0.0.1:8000/ready

          for _ in $(seq 1 60); do
            if curl --fail --silent http://127.0.0.1:3000/ >/dev/null; then
              break
            fi
            sleep 2
          done
          curl --fail --silent http://127.0.0.1:3000/ >/dev/null

          test "$(docker compose -f compose.yaml ps --services --status running | wc -l)" -eq 5
          test "$(docker compose -f compose.yaml exec -T api id -u)" != "0"
          test "$(docker compose -f compose.yaml exec -T worker id -u)" != "0"
          docker compose -f compose.yaml logs worker | grep -q 'worker started'

      - name: Show service state
        if: always()
        run: docker compose -f compose.yaml ps

      - name: Cleanup
        if: always()
        run: docker compose -f compose.yaml down -v --remove-orphans
